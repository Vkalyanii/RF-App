_schema-version: "3.2"
ID: nsrfapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nsrfapp-destination-content
  type: com.sap.application.content
  requires:
  - name: nsrfapp-destination-service
    parameters:
      content-target: true
  - name: nsrfapp-repo-host
    parameters:
      service-key:
        name: nsrfapp-repo-host-key
  - name: nsrfapp-uaa
    parameters:
      service-key:
        name: nsrfapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nsrfapp_html_repo_host
          ServiceInstanceName: nsrfapp-html5-service
          ServiceKeyName: nsrfapp-repo-host-key
          sap.cloud.service: nsrfapp
        - Authentication: OAuth2UserTokenExchange
          Name: nsrfapp_uaa
          ServiceInstanceName: nsrfapp-xsuaa-service
          ServiceKeyName: nsrfapp-uaa-key
          sap.cloud.service: nsrfapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: nsrfapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nsrfapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsrfapp.zip
      name: nsrfapp
      target-path: resources/
- name: nsrfapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nsrfapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nsrfapp-destination-service
    service-plan: lite
- name: nsrfapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nsrfapp-xsuaa-service
    service-plan: application
- name: nsrfapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nsrfapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
